<%- include('partials/header'); -%>
<h1>Home</h1>

<p><%= homeContent %></p>

    <% postArray.forEach(element => {%>
        <h1><%= element.title %></h1>
        <p class="limited"><%= element.content %></p>
        <% }); %>
    
 
<%- include('partials/footer'); -%>
